
function getExactConflictAverage(size, aggregator) {

    foreach (instance in instanceGenerator(size, isNotPartitioned)) {
        aggregator.aggregate(getConflictCount(instance))
    }

    return aggregator.result()
}